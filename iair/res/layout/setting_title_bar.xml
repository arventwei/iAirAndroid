<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="@dimen/title_bar_height"
    android:background="@drawable/bg_application_bar"
    android:gravity="center_vertical" >

    <ImageView
        android:id="@+id/return_btn"
        android:layout_width="@dimen/title_bar_btn_width"
        android:layout_height="fill_parent"
        android:background="@drawable/weather_detail_activity_btn_bg"
        android:src="@drawable/title_bar_return" />

    <TextView
        android:id="@+id/title"
        android:layout_width="0.0dip"
        android:layout_height="wrap_content"
        android:layout_marginLeft="5.0dip"
        android:layout_weight="1.0"
        android:text="@string/setting_main_title"
        android:textColor="@color/title_bar_text"
        android:textSize="@dimen/title_bar_title_text_size" />

</LinearLayout>